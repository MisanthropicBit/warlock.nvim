local plugins = {}

function plugins.create(colors)
    return {
        -- Gitsigns
        -- Neogit
        -- Treesitter
        -- Telescope
        -- Dashboard
        -- DapUI
        -- Cmp
        -- IndentBlankline
        -- diffview
        -- todo-comments
    }
end

return plugins
